class Restaurant {
  constructor(id, name, address, description, tags) {
    this.id = id;
    this.name = name;
    this.address = address;
    this.description = description;
    this.tags = tags;
  }
}

export default Restaurant;
